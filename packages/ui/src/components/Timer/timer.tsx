interface TimerProps {
  timeRemaining: number;
  totalTime: number;
  interval: number;
}

export default function Timer({
  timeRemaining,
  totalTime,
  interval,
}: TimerProps) {
  return (
    <>
      <div></div>
    </>
  );
}
